---
import PageLayout from '../../layouts/PageLayout.astro';
import { frameworks } from '../../data/messaging';
---

<PageLayout
  title="Frameworks"
  description="Two frameworks for responsible AI. The Four Lenses assesses whether AI tools serve people. The Lift guides teams through adoption that lifts everyone. No Human Left Behind."
>
  <article>
    <section class="section-cream section" aria-labelledby="fw-heading">
      <div class="container">
        <p class="label label-gold">Human-Centered AI Adoption</p>
        <h1 id="fw-heading" class="fw-title">Frameworks</h1>
        <p class="fw-lede">
          Adopting AI responsibly requires more than learning new tools. It requires
          trust, a new way of thinking, and a new way of working. These
          frameworks guide that shift.
        </p>
        <div class="fw-grid">
          <div class="fw-entry">
            <p class="fw-type">AI Adoption</p>
            <h2 id="the-lift-heading" class="fw-name">
              <a href="/frameworks/the-lift" class="link">The Lift</a>
            </h2>
            <p class="fw-desc">
              {frameworks.lift.description}
            </p>
          </div>

          <div class="fw-entry">
            <p class="fw-type">Responsible AI Assessment</p>
            <h2 id="four-lenses-heading" class="fw-name">
              <a href="/frameworks/four-lenses" class="link">The Four Lenses</a>
            </h2>
            <p class="fw-desc">
              {frameworks.fourLenses.description}
            </p>
          </div>
        </div>
      </div>
    </section>
  </article>
</PageLayout>

<style>
  .fw-title {
    font-family: var(--font-heading);
    font-size: var(--text-4xl);
    line-height: 1.1;
  }

  .fw-lede {
    font-size: var(--text-xl);
    line-height: 1.5;
    color: var(--color-text-secondary);
    max-width: var(--max-width-prose);
    margin-bottom: var(--space-lg);
  }


  .fw-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-2xl);
  }

  .fw-entry {
    border-top: 1px solid var(--color-gray-border);
    padding-top: var(--space-md);
  }

  .fw-type {
    font-size: var(--text-xs);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-gold-dark);
    margin-bottom: var(--space-2xs);
  }

  .fw-name {
    font-family: var(--font-heading);
    font-size: var(--text-3xl);
    font-weight: 700;
    margin-bottom: var(--space-sm);
  }

  .fw-desc {
    font-size: var(--text-base);
    line-height: 1.65;
    color: var(--color-text-secondary);
    margin-bottom: var(--space-xs);
  }

  @media (max-width: 768px) {
    .fw-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
