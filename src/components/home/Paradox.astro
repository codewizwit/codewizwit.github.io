---
/**
 * The Paradox section — near-black background.
 * "More AI. More human." — full stop.
 */
---

<section class="paradox section-cream section" aria-labelledby="paradox-heading">
  <div class="container">
    <h2 id="paradox-heading" class="paradox-heading reveal">
      More AI. More human.
    </h2>
    <p class="paradox-body reveal">
      AI handles the noise. You handle what matters. The more we automate the
      repetitive, the more space we create for collaboration, creativity, and
      the kind of care that only humans bring.
    </p>
  </div>
</section>

<style>
  .paradox {
    display: flex;
    align-items: center;
    min-height: 60vh;
  }

  .paradox-heading {
    font-family: var(--font-heading);
    font-size: var(--text-5xl);
    font-weight: 700;
    line-height: 1.05;
    letter-spacing: -0.025em;
    color: var(--color-text);
    margin-bottom: var(--space-md);
  }

  .paradox-body {
    font-size: var(--text-xl);
    line-height: 1.5;
    color: var(--color-text-secondary);
    max-width: var(--max-width-prose);
  }
</style>
