---
/**
 * The Paradox section. Cream background.
 * "More AI. More human." with typing animation.
 */
import TypingAnimation from '../TypingAnimation.astro';
---

<section class="paradox section-cream section" aria-labelledby="paradox-heading">
  <div class="container">
    <h2 id="paradox-heading" class="paradox-heading reveal">
      <TypingAnimation static="More AI." typed=" More human." id="paradox" />
    </h2>
    <p class="paradox-body reveal">
      AI handles the noise. We handle what matters. The more we automate the
      repetitive, the more space we create for collaboration, creativity, and
      the kind of care that only humans bring.
    </p>
  </div>
</section>

<style>
  .paradox {
    display: flex;
    align-items: center;
    min-height: 40vh;
  }

  .paradox .container {
    text-align: center;
  }

  .paradox-heading {
    font-family: var(--font-heading);
    font-size: var(--text-5xl);
    font-weight: 700;
    line-height: 1.05;
    letter-spacing: -0.025em;
    color: var(--color-text);
    margin-bottom: var(--space-md);
  }

  .paradox-body {
    font-size: var(--text-xl);
    line-height: 1.5;
    color: var(--color-text-secondary);
    max-width: var(--max-width-prose);
    margin-inline: auto;
  }
</style>
